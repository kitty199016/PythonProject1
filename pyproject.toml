[project]
name = "pythonproject1"
version = "0.1.0"
description = ""
authors = [
    {name = "Екатерина Калингер",email = "kalinger90@list.ru"}
]
package-mode = false
requires-python = "^3.14"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)"
]
[tool.pytest.ini_options]
pythonpath = ["src"]